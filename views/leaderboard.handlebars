{{!-- <article>
    <h2>Leaderboard<br>
        <img class="reg-image" src="/images/big_hill.jpg" alt="Biking up a long hill." />
    </h2><br>
</article> --}}

<article>
    <h2>2021 Leaderboards</h2>
    <h4>Overall</h4>
    <table class="leaderboard">
        <tr>
            <th>Rider Name</th>
            <th>Elevation</th>
            <th>Miles</th>
            <th>Routes</th>
            <th>Points</th>
        </tr>
        {{#each userPoints.userPoints as |userPoints|}}
        <tr>
            <td>{{userPoints.user.name}}</td>
            <td>{{format_thousands userPoints.total_elevation}}</td>
            <td>{{format_miles userPoints.total_miles}}</td>
            <td>{{userPoints.total_routes}}</td>
            <td class="points">{{userPoints.total_points}}</td>
        </tr>
        {{/each}}
    </table>
</article>

{{!-- FeMale Overall Leaderboard --}}
<article>
    <h4>Female</h4>
    <table class="leaderboard">
        <tr>
            <th>Rider Name</th>
            <th>Elevation</th>
            <th>Miles</th>
            <th>Routes</th>
            <th>Points</th>
        </tr>
        {{#each userPoints.userPoints as |userPoints|}}
        <tr>
            <td>{{userPoints.user.name}}</td>
            <td>{{format_thousands userPoints.total_elevation}}</td>
            <td>{{format_miles userPoints.total_miles}}</td>
            <td>{{userPoints.total_routes}}</td>
            <td class="points">{{userPoints.total_points}}</td>
        </tr>
        {{/each}}
    </table>
</article>

{{!-- Male Overall Leaderboard --}}
<article>
    <h4>Male</h4>
    <table class="leaderboard">
        <tr>
            <th>Rider Name</th>
            <th>Elevation</th>
            <th>Miles</th>
            <th>Routes</th>
            <th>Points</th>
        </tr>
        {{#each userPoints.userPoints as |userPoints|}}
        <tr>
            <td>{{userPoints.user.name}}</td>
            <td>{{format_thousands userPoints.total_elevation}}</td>
            <td>{{format_miles userPoints.total_miles}}</td>
            <td>{{userPoints.total_routes}}</td>
            <td class="points">{{userPoints.total_points}}</td>
        </tr>
        {{/each}}
    </table>
</article>